## Problem 1 (revised) — Sensor Data Cleanup (Easy–Medium)

You're given a NumPy array `data` of shape `(T, C)`, representing `T` timesteps of sensor readings from `C` sensors on a physical test rig (e.g., pressure sensors on a wind tunnel model). The data has issues:

- Some entries are `NaN` (sensor dropouts).
- Some entries are extreme outliers (sensor glitches) — values more than `k` standard deviations away from that sensor's mean.

Implement:

```python
import numpy as np

def clean_sensor_data(data: np.ndarray, k: float = 3.0) -> np.ndarray:
    """
    Clean a (T, C) array of sensor readings.

    Steps:
      1. Replace outliers (|x - mean_c| > k * std_c, computed per column
         IGNORING NaNs) with NaN.
      2. Fill all NaNs by linear interpolation along the time axis,
         per sensor column.
      3. If a column starts or ends with NaNs, fill them with the
         nearest valid value (forward/backward fill at the edges).

    Returns a (T, C) array with no NaNs.
    """
```

### Requirements

1. Vectorized where reasonable — a loop over the `C` columns is acceptable since `C` is typically small (say, < 1000), but the operations *within* a column should be vectorized.
2. Robust to columns that are entirely `NaN`? You can assume no column is entirely `NaN`, but mention it if you notice the assumption.
3. Clean, readable code.

### What I'm looking for

- Correct handling of `NaN` in statistics (`np.nanmean`, `np.nanstd`).
- A sensible interpolation approach (`np.interp` is your friend).
- Edge handling at the start/end of a column.

Take your time — aim for **10–15 minutes**, but don't stress the clock. Paste your solution when ready and I'll review.