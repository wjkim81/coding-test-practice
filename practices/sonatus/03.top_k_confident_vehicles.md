## 첫 문제 (3개 중 1번)

### [Setting]
Sonatus AI Director가 차량 fleet에서 inference 결과를 받고 있어. 각 차량은 자기 ID와 함께 prediction confidence score를 stream으로 보내. 너는 "**가장 높은 confidence를 받은 차량 top K개**"를 추적하는 함수를 짜야 해.

### [Problem]
```python
def top_k_confident_vehicles(
    scores: list[tuple[str, float]],  # [(vehicle_id, confidence), ...]
    k: int
) -> list[str]:
    """
    Return top-k vehicle_ids sorted by confidence (highest first).
    
    Note: A vehicle_id can appear multiple times in scores.
    Use the HIGHEST confidence ever reported for each vehicle.
    """
    pass
```

### [Example]
```python
scores = [
    ("v1", 0.7),
    ("v2", 0.9),
    ("v1", 0.85),   # v1's confidence updated to 0.85
    ("v3", 0.6),
    ("v2", 0.5),    # v2's max is still 0.9
    ("v4", 0.95),
]
k = 2
# Expected: ["v4", "v2"]  (0.95, 0.9)
```

### [Constraints]
- `len(scores)` up to 1,000,000
- Unique vehicles up to 100,000
- Optimize for time complexity

### [Initial questions]

Clarifying 1-2개만. 그러고 바로 접근 방식 한 줄로 commit하고 코딩 시작. 

(힌트 미리: 두 가지 접근이 떠올라야 해. 어느 게 더 efficient한지 reasoning까지.)

자, 가봐 🚀