```
[Setting]
차량 fleet에서 각 차량의 일별 max speed가 list로 들어옴. 
OEM이 알고 싶은 거: "각 날짜 D에 대해, 그 차량의 max speed를 처음으로 넘어선 미래 날짜는?"
(없으면 -1)

[Problem]
def days_until_higher_speed(daily_max_speeds: list[float]) -> list[int]:
    """
    For each index i, return the number of days until a higher speed appears.
    If no higher speed in future, return -1.
    """

[Example]
speeds = [100, 80, 110, 105, 120, 90]
# i=0 (100): next higher = i=2 (110). → 2 days
# i=1 (80):  next higher = i=2 (110). → 1 day
# i=2 (110): next higher = i=4 (120). → 2 days
# i=3 (105): next higher = i=4 (120). → 1 day
# i=4 (120): never. → -1
# i=5 (90):  never. → -1
# Result: [2, 1, 2, 1, -1, -1]
```

이거 LeetCode #739 "Daily Temperatures" 의 변형이야. 너 풀어본 거 같으면 *"아 이거"* 할 수도.

**질문**:
1. 이 문제 패턴 인식. Monotonic stack이 떠올라? 떠오르면 어떤 logic으로 갈지 한두 문장.
2. 코딩까지 가도 되고, 패턴 사고만 정리해도 OK. 너 컨디션 따라.

가볍게 가자 ㅎㅎ