**문제: Implement Stack using Queues**

반대로! **Queue로 Stack**을 구현하세요.

```python
s = MyStack()
s.push(1)    # [1]
s.push(2)    # [1, 2]
s.top()      # 2 (맨 위)
s.pop()      # 2 반환
s.empty()    # False
```

**힌트:** push할 때 트릭! 새 원소 넣고, 앞에 있는 걸 전부 뒤로 돌리면?
