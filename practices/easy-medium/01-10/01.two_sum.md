**문제: Two Sum**

정렬되지 않은 정수 배열 `nums`와 정수 `target`이 주어졌을 때, 합이 `target`이 되는 두 수의 **인덱스**를 반환하세요.

```
Input: nums = [2, 7, 11, 15], target = 9
Output: [0, 1]  # because nums[0] + nums[1] = 9
```

**조건:**
- 정답은 정확히 하나 존재
- 같은 원소를 두 번 사용하면 안 됨
- O(n) 풀이를 목표로

준비되면 코드 보내줘! 풀고 나면 바로 다음 문제 갈게.