**문제: Product of Array Except Self**

정수 배열 `nums`가 주어질 때, `result[i]`는 `nums[i]`를 **제외한** 나머지 모든 원소의 곱.

**나누기 연산 사용 금지!**

```
Input:  [1, 2, 3, 4]
Output: [24, 12, 8, 6]

Input:  [2, 3, 4, 5]
Output: [60, 40, 30, 24]
```

**힌트:** 왼쪽에서 누적곱, 오른쪽에서 누적곱 — 두 번 sweep!