"""
좋아요. **그 순서 선택이 아주 합리적**입니다.
DFS/BFS는 기억 복구가 필요하고, **Day 5(Interval 계열)**은 지금 감각으로 바로 흡수할 수 있는 파트예요.

아래처럼 **Day 5를 한 번에 정리**하고 나서 DFS/BFS로 가면 훨씬 덜 좌절합니다.

---

# Coding Test — **Day 5 (Intervals / Sorting + Sweep)**

이 날의 핵심은 한 줄입니다.

> **“정렬한 뒤, 앞에서부터 겹치는지를 판단하며 상태를 갱신한다.”**

문자열 ❌
슬라이딩 윈도우 ❌
재귀 ❌

→ **정렬 + 조건 분기 + 상태 유지**

---

## Day 5 – Problem 1

## **Merge Intervals** (Medium)

### 문제

구간 리스트 `intervals`가 주어질 때,
**겹치는 구간을 모두 병합**하여 반환하라.

* 각 interval = `[start, end]`
* 입력은 정렬되어 있지 않음

### 예시

```text
intervals = [[1,3],[2,6],[8,10],[15,18]]
→ [[1,6],[8,10],[15,18]]
```

```text
intervals = [[1,4],[4,5]]
→ [[1,5]]
```

---

### 코테에서 보는 핵심

* 왜 정렬이 필요한가?
* 겹침(overlap)을 어떻게 정의하는가?
* 상태 변수(현재 interval)를 어떻게 유지하는가?

---

### 핵심 아이디어 (이것만 이해하면 끝)

1. **start 기준으로 정렬**
2. 결과 리스트의 마지막 interval과 비교
3. 겹치면 → end 확장
4. 안 겹치면 → 새 interval 추가

겹침 조건:

```text
current.start <= last.end
```

---

### 요구사항

```python
def merge(intervals: list[list[int]]) -> list[list[int]]:
    ...
```

---



---

## 오늘 진행 방식 (추천)

### Step 1️⃣

👉 **Merge Intervals 먼저**

* 이 문제 하나로:

  * 정렬
  * sweep
  * 상태 갱신
    전부 익힐 수 있음

### Step 2️⃣

Meeting Rooms는 **5분 컷**으로 자연스럽게 따라옴

---

## 중요한 멘탈 포인트 (아주 중요)

이 Day 5 문제들은:

* “연습량 게임” ❌
* “사고 흐름 이해” ⭕⭕⭕

그래서 **지금 당신한테 가장 잘 맞는 날**입니다.

---

## 바로 시작하자

👉 **Problem 1: Merge Intervals**
코드부터 작성해서 붙여주세요.

(정렬 → loop → last interval 업데이트
이 흐름만 지키면 됩니다.)

"""

# ChatGPT 풀이
def merge(intervals: list[list[int]]) -> list[list[int]]:
    if not intervals:
        return []

    intervals.sort(key=lambda x: x[0])
    out = [intervals[0][:]]  # copy

    for i in range(1, len(intervals)):
        curr_start, curr_end = intervals[i]
        last_end = out[-1][1]

        if curr_start <= last_end:
            out[-1][1] = max(last_end, curr_end)
        else:
            out.append([curr_start, curr_end])

    return out


if __name__ == "__main__":

    # intervals = [[1,3],[2,6],[8,10],[15,18]]
    intervals = [[1,4],[4,5]]
    print(intervals)

    print(merge(intervals))